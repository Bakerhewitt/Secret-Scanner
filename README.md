# Secret Scanner
Baker Hewitt

## Overview

**Secret Scanner** is a regex scanner that mines for "secrets" within an inputted file or directory.

**Video Demonstration** https://youtu.be/NOhxoq9C02c 

## Features
- **Dual Scan Modes**: Can scan either a single file or an entire directory recursively.
- **Regex-based Detection**: Uses pattern-matching to find "secrets" such as passwords, API keys, and tokens from Datadog, Square, and Twilio.
- **Reporting**: Prints findings with filename, line number, and matched string.
- **CLI Interface**: Can be run interactively or via command line.
- **Logging**: Tracks scan activity.

## Requirements

Python Interpreter

## Disclaimer
**AI Usage** The final version was completed with the assistance of Claude, as I was struggling to fill the gaps necessary for the CLI interface and logging. For exact updates and changes, please compare both versions in the History section of the file.
